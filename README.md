# vimconfig
Instalacion:
```
cd ~
git clone https://github.com/blasrodriguez/vimconfig.git
ln -s ~/vimconfig/.vimrc ~/.vimrc
mkdir -p ~/.vim/autoload
curl -LSso ~/.vim/autoload/pathogen.vim https://tpo.pe/pathogen.vim
ln -s ~/vimconfig/bundle ~/.vim/bundle
```
